<x-restaurant-layout title="Gestion de la carte" :admin="true"><div class="section-heading"><div><p class="eyebrow">VOTRE SAVOIR-FAIRE À LA CARTE</p><h1>{{ ['product' => 'Les plats', 'menu' => 'Les menus', 'offer' => 'Les offres'][$kind] }}</h1></div><a class="button" href="{{ route('admin.catalog.create', $kind) }}">Créer {{ ['product' => 'un plat', 'menu' => 'un menu', 'offer' => 'une offre'][$kind] }} +</a></div><div class="order-list">@forelse($items as $item)<a class="panel order-summary" href="{{ route('admin.catalog.edit', [$kind, $item->id]) }}"><div><h3>{{ $item->name }}</h3><p>{{ Str::limit($item->description, 100) }}</p></div><span class="badge">{{ $item->available ? 'Activé' : 'Indisponible' }}</span><strong>{{ number_format($item->price / 100, 2, ',', ' ') }} € · Modifier ↗</strong></a>@empty<div class="empty-state"><h2>Votre carte commence ici</h2><p>Créez votre premier article pour le proposer aux clients.</p></div>@endforelse</div></x-restaurant-layout>
